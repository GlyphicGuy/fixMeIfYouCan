{
  "name": "fix-me-if-you-can",
  "version": "1.0.0",
  "description": "A compromised cipher terminal. Fix the bugs to decode the secret message.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "6.4.1"
  }
}
